{
  "type": "module",
  "scripts": {
    "fetch": "node --env-file-if-exists=.env fetch.ts",
    "analyse": "node analyse.ts",
    "build": "node build.ts",
    "strategy": "node --env-file-if-exists=.env strategy.ts"
  },
  "dependencies": {
    "viem": "^2"
  },
  "devDependencies": {
    "@types/node": "latest"
  }
}
